/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package project2outline;

/**
 *
 * @author lukasbernard
 * class for the red black tree object
 */
public class RBT {
    /**
     * @param Node
     * this fnction finds the grandparents other child if one exists and returns it
     * @return Node's uncle
     */
    public Node getUncle()
    {
        return node;
    }
    /**
     * @param key and value
     * this function takes in the key and value and creates a new node with them
     * then it proceeds to place the node in its correct spot through the recursive place method
     */
    public void put()
    {
        
    }
    /**
     * 
     * @param t
     * @param p 
     * this takes the two nodes, compares them and places our initial node in the correct place, left or right
     */
    public void place(Node t, node p)
    {
        
    }
    /**
     * flip color decides if the node needs its color changed then proceeds to change the color of all the nodes that
     * are colored incorrectly by the rule of red black trees
     * @param temp 
     */
    public void flipcolor(node temp)
    {
        
    }
    
    /**
     * This method rotates subtrees left over the temp node if necessary by looking at all the nodes around our temp node
     * @param temp 
     */
    public void rotateLeft(node temp)
    {
        
    }
    /**
     * This method rotates a subtree right over the temp node if necessary to keep the red black tree rules in check
     * @param temp 
     */
    public void rotateRight(node temp)
    {
        
    }
}
